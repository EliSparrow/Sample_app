<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to (gravatar_for user, size: 50), user %>
      <%= user.first_name %> <%= user.name %>
    </li>
  <% end %>
</ul>